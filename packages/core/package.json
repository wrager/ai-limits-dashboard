{
  "name": "@ai-limits/core",
  "version": "0.1.0-alpha.2",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "~5.6.0",
    "vitest": "^2.1.0"
  }
}
