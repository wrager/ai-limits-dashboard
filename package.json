{
  "name": "ai-limits-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r run dev --parallel",
    "build": "pnpm -r run build",
    "lint": "eslint .",
    "typecheck": "pnpm -r run typecheck",
    "test": "vitest run",
    "e2e": "echo 'E2E: run after Tauri scaffold (tauri-driver + WebdriverIO)'"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@vitest/coverage-v8": "^2.1.0",
    "eslint": "^9.15.0",
    "typescript": "~5.7.2",
    "typescript-eslint": "^8.16.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.14.2"
}
